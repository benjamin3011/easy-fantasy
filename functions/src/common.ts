// src/common.ts

// If you have interfaces or helper functions used across multiple
// feature files (e.g., dataSync, leagues), define them here.

// Example (if needed later):
// export interface UserProfile {
//   firstName: string;
//   lastName: string;
//   email: string;
//   // ... other fields
// }

// Currently, no shared items identified besides config constants,
// so this file might remain empty or be omitted for now.

// You could potentially move the Tank01 API interfaces here if
// multiple function groups need them, but keeping them in dataSync.ts
// seems reasonable for now.

